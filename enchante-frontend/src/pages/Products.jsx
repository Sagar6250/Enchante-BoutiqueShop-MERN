import { RootContainer } from "../components/layout";

const Products = () => {
    return (
        <RootContainer>
            <div>Products</div>
        </RootContainer>
    );
};

export default Products;
